import XCTest
@testable import swift_argument_encoding

final class swift_argument_encodingTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_argument_encoding().text, "Hello, World!")
    }
}
